# Fichier d’environnement Mobile (.env) — exemple FR
# Copiez/renommez ce fichier en ".env" dans le dossier mobile/
# Les variables EXPO_PUBLIC_* sont exposées au client (ne jamais y mettre de secrets)

# URL de l’API Backend (dev)
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000

# Locale et devise par défaut (optionnel)
EXPO_PUBLIC_DEFAULT_LOCALE=fr
EXPO_PUBLIC_DEFAULT_CURRENCY=XAF
